import React from 'react';
import classes from './Contacts.css';

class Contacts extends React.Component {
    render() {
        return(
            <div className={classes.ContactsContainer}>Contacts</div>
        );
    }
}

export default Contacts;